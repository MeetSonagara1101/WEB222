// Function expression
function area(radius) {
    const area = Math.PI * radius ** 2;
}

// Function call
const circleArea = calculateArea(5);
console.log(`The area of a circle with radius 5 is ${circleArea}`);